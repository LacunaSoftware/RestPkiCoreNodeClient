export interface SignatureSessionDocumentData {
    
    // id : string;
    // file : ;
    // mimeType : model['mimeType'] || null;
    // content : model['content'] || null;
    // blobToken : model['blobToken'] || null;
    // url : model['url'] || null;
    // name : model['name'] || null;
    // length : model['length'] || null;
    
}